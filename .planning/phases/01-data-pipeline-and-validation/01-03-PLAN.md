---
phase: 01-data-pipeline-and-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - DATA-03

must_haves:
  truths:
    - "All pipeline code and test stubs are committed to GitHub before the user places the dataset zip"
    - "The user has placed nfl-big-data-bowl-2026-prediction.zip in the project root"
    - "The project root contains no tracked data files (dataset is in .gitignore)"
  artifacts:
    - path: ".git"
      provides: "Git repository with Wave 1 code committed"
      contains: "src/data/loader.py, src/data/preprocessor.py"
  key_links:
    - from: "git commit"
      to: "GitHub remote"
      via: "git push"
      pattern: "All source committed before dataset upload"
---

<objective>
Enforce DATA-03: commit all Wave 1 code to GitHub, then prompt the user to place the dataset zip in the project root.

Purpose: DATA-03 is an explicit project requirement — the codebase must be committed before the dataset is ever uploaded. This plan is the hard gate between "code-only" waves and "data-present" waves.

Output: GitHub remote updated with all Wave 1 code; dataset zip placed in project root by the user, ready for pipeline execution.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-pipeline-and-validation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit all Wave 1 code to GitHub</name>
  <files>
    pyproject.toml
    .gitignore
    pytest.ini
    src/
    tests/
    scripts/
  </files>
  <action>
Stage and commit all files created in plans 01-01 and 01-02. Then push to the remote.

Run the following in the project root:

```bash
cd "/Users/rahulb/Desktop/Personal Projects/Defensive Trajectory Prediction Model"
git add pyproject.toml .gitignore pytest.ini
git add src/ tests/ scripts/ data/.gitkeep outputs/
git status
git commit -m "feat(phase-1): scaffold pipeline — loader, preprocessor, sample_builder, test stubs"
git push
```

If the remote is not configured yet, instruct the user to create a new GitHub repository and run:
```bash
git remote add origin https://github.com/USERNAME/REPO.git
git branch -M main
git push -u origin main
```

Verify the push succeeded by running `git log --oneline -3` and confirming the commit appears.
  </action>
  <verify>
    <automated>cd "/Users/rahulb/Desktop/Personal Projects/Defensive Trajectory Prediction Model" && git log --oneline -1 && git status --short 2>&1</automated>
  </verify>
  <done>git log shows the commit; git status shows no uncommitted changes (clean working tree)</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User places dataset zip in project root</name>
  <what-built>
    All pipeline code is committed to GitHub. The project now has:
    - src/data/loader.py — zip extraction + CSV loading
    - src/data/preprocessor.py — normalization, interpolation, splitting
    - src/data/sample_builder.py — tensor construction
    - scripts/run_pipeline.py — end-to-end runner
    - tests/test_pipeline.py — 8 tests (7 pass against synthetic fixtures)

    DATA-03 is satisfied: the codebase exists on GitHub before any data upload.
  </what-built>
  <instructions>
    Place the competition dataset zip in the project root directory:

    **Required:** Copy `nfl-big-data-bowl-2026-prediction.zip` to:
    `/Users/rahulb/Desktop/Personal Projects/Defensive Trajectory Prediction Model/nfl-big-data-bowl-2026-prediction.zip`

    The file must be named exactly `nfl-big-data-bowl-2026-prediction.zip`.

    Once placed, confirm by running:
    ```bash
    ls -lh "/Users/rahulb/Desktop/Personal Projects/Defensive Trajectory Prediction Model/nfl-big-data-bowl-2026-prediction.zip"
    ```

    The file is already in .gitignore and will not be committed.
  </instructions>
  <resume-signal>Type "dataset ready" once the zip file is in place and the ls command shows it</resume-signal>
</task>

</tasks>

<verification>
After the user confirms dataset placement:
```bash
ls -lh "/Users/rahulb/Desktop/Personal Projects/Defensive Trajectory Prediction Model/nfl-big-data-bowl-2026-prediction.zip"
git status --short
```
Expected: zip file shows size > 0, git status shows no tracked changes (zip is gitignored).
</verification>

<success_criteria>
- Git remote contains all source files from plans 01-01 and 01-02
- nfl-big-data-bowl-2026-prediction.zip exists in the project root
- git status is clean (zip not tracked)
- DATA-03 is satisfied: code-first, data-second
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-and-validation/01-03-SUMMARY.md`
</output>
