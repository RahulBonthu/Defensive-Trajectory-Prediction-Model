---
phase: 03-model-architecture-and-training-infrastructure
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_model.py
  - src/model/__init__.py
  - scripts/overfit_test.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05, MODEL-06]
must_haves:
  truths:
    - "8 tests in tests/test_model.py all fail (RED) before any model implementation exists"
    - "src/model/__init__.py exists enabling `from src.model.trajectory_model import TrajectoryTransformer` to produce an ImportError (not a ModuleNotFoundError on the package)"
    - "scripts/overfit_test.py exists as a runnable script that fails with ImportError until the model is implemented"
    - "Every MODEL-01 through MODEL-06 requirement has at least one test stub"
  artifacts:
    - path: "tests/test_model.py"
      provides: "8 RED test stubs covering MODEL-01 (×2), MODEL-02, MODEL-03, MODEL-04, MODEL-05, MODEL-06, SC-4"
      min_lines: 80
    - path: "src/model/__init__.py"
      provides: "Empty module init — enables package import"
    - path: "scripts/overfit_test.py"
      provides: "Overfit verification script stub for SC-2"
  key_links:
    - from: "tests/test_model.py"
      to: "src/model/trajectory_model.py"
      via: "from src.model.trajectory_model import TrajectoryTransformer"
      pattern: "from src\\.model\\.trajectory_model import"
---

<objective>
Write RED test stubs and module skeleton before any model implementation exists.

Purpose: Establish the TDD contract for the model architecture phase. Every MODEL requirement must have a failing test that defines exactly what the implementation must satisfy. The stubs also create the file structure that Plan 03-02 will fill in.
Output: tests/test_model.py (8 RED stubs), src/model/__init__.py (empty init), scripts/overfit_test.py (runnable stub that fails on missing import)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-model-architecture-and-training-infrastructure/03-RESEARCH.md
@.planning/phases/03-model-architecture-and-training-infrastructure/03-VALIDATION.md

<interfaces>
<!-- Key constants from src/data/dataset.py that tests should reference -->
From src/data/dataset.py:
```python
SEQUENCE_LENGTH: int = 25               # default T  (input sequence length)
# Model A input_dim = 50  (own kinematics 8 + social context 21*2 = 42 = 50)
# Model B input_dim = 52  (50 + ball_land_x + ball_land_y)
```
Input shapes:
- Model A: (batch, T=25, input_dim=50)
- Model B: (batch, T=25, input_dim=52)
- Output both: (batch, 2)
- padding_mask: (batch, T) bool, True=real frame, False=padded frame
</interfaces>
</context>

<feature>
  <name>TrajectoryTransformer TDD stubs</name>
  <files>tests/test_model.py, src/model/__init__.py, scripts/overfit_test.py</files>
  <behavior>
    All 8 tests in test_model.py MUST be RED (failing) before implementation:

    test_forward_pass_model_a:
      - Instantiate TrajectoryTransformer(input_dim=50)
      - Forward pass with dummy (2, 25, 50) tensor and all-True (2, 25) mask
      - Assert output shape == (2, 2)

    test_forward_pass_model_b:
      - Instantiate TrajectoryTransformer(input_dim=52)
      - Forward pass with dummy (2, 25, 52) tensor and all-True (2, 25) mask
      - Assert output shape == (2, 2)

    test_conv_output_shape:
      - Instantiate TrajectoryTransformer(input_dim=50)
      - Run forward with (1, 25, 50) input
      - Assert sequence length T=25 is preserved (via output shape — model internally preserves T)
      - Assert output shape == (1, 2)

    test_encoder_with_padding_mask:
      - Instantiate TrajectoryTransformer(input_dim=50)
      - Create (2, 25, 50) input; set last 5 frames of first sample to padding (mask False)
      - Forward pass must not raise; output shape == (2, 2)

    test_output_shape:
      - Instantiate TrajectoryTransformer(input_dim=50)
      - Assert output has exactly 2 dimensions: (batch=4, 2)
      - No extra dims, dtype float

    test_model_a_config:
      - Instantiate TrajectoryTransformer(input_dim=50)
      - Assert model.conv.in_channels == 50
      - This is the "config inspection" test (MODEL-05)

    test_model_b_config:
      - Instantiate TrajectoryTransformer(input_dim=52)
      - Assert model.conv.in_channels == 52
      - This is the "config inspection" test (MODEL-06)

    test_padding_mask_attention:
      - Instantiate TrajectoryTransformer(input_dim=50) using AttentionCapturingEncoderLayer
        (or a get_attention_weights helper on the model)
      - Create (1, 25, 50) input; set frames 10-24 to padding (mask False for those positions)
      - Run forward pass
      - Extract attention weights from the first encoder layer
      - Assert that mean attention weight on padded columns (10-24) < 0.05
        (near-zero — padded tokens are ignored)

    Overfit test stub (scripts/overfit_test.py):
      - Import TrajectoryTransformer and rmse_loss
      - Fail immediately with ImportError until trajectory_model.py exists
      - The full implementation will run the 100-sample loop and assert final_loss < initial_loss * 0.5
  </behavior>
  <implementation>
    RED phase: Write tests that import from src.model.trajectory_model which does not yet exist.
    All 8 tests will fail with ImportError. That is the expected RED state.

    src/model/__init__.py: empty file only. Do NOT create trajectory_model.py — that is Plan 03-02.

    scripts/overfit_test.py: Write the FULL overfit script (not just a stub), including:
    - get_device() using CUDA → MPS → CPU selector
    - 100-sample synthetic dataset for both input_dim=50 and input_dim=52
    - 200-epoch training loop with Adam lr=1e-3 and dropout=0.0
    - rmse_loss using torch.sqrt(mse + 1e-6)
    - Assertion: final_loss < initial_loss * 0.5 for both variants
    - Print which device was selected
    Since trajectory_model.py will not exist yet, the script will fail on import — that is fine.
    Writing it now means Plan 03-02 just needs to make the import work.

    TDD commit sequence:
    1. RED commit: `test(03-01): add 8 RED stubs for MODEL-01–MODEL-06 + SC-4`
       Verify: `pytest tests/test_model.py` shows 8 ERRORS (ImportError) — all RED

    No GREEN or REFACTOR phase — this plan is RED-only. GREEN is Plan 03-02.
  </implementation>
</feature>

<verification>
RED state confirmed:
```
pytest tests/test_model.py -v
```
Expected: 8 errors (ImportError: cannot import name 'TrajectoryTransformer' from 'src.model.trajectory_model')
All 8 tests must be failing — any passing test indicates a stub was not written correctly.

Existing tests unaffected:
```
pytest tests/test_pipeline.py tests/test_dataset.py -v
```
Expected: 14 tests pass (Phase 1 + Phase 2 baseline).
</verification>

<success_criteria>
- tests/test_model.py exists with exactly 8 test functions
- src/model/__init__.py exists (empty)
- scripts/overfit_test.py exists with the full overfit loop written (importable once trajectory_model.py exists)
- `pytest tests/test_model.py` shows 8 errors (ImportError) — RED state confirmed
- `pytest tests/test_pipeline.py tests/test_dataset.py` still shows 14 passing
</success_criteria>

<output>
After completion, create `.planning/phases/03-model-architecture-and-training-infrastructure/03-01-SUMMARY.md`
</output>
